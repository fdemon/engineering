- error = @worker.errors.include?(:name)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :name, :class => 'control-label'
  .controls
    =f.text_field :name, :class => 'input-xlarge input-with-feedback', :autofocus => true
    %span.help-inline=error ? f.error_message_on(:name, :class => 'text-error') : pat(:example)
- error = @worker.errors.include?(:worker_type)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :worker_type, :class => 'control-label'
  .controls
    =f.text_field :worker_type, :class => 'input-xlarge input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:worker_type, :class => 'text-error') : pat(:example)
- error = @worker.errors.include?(:phone)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :phone, :class => 'control-label'
  .controls
    =f.text_field :phone, :class => 'input-xlarge input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:phone, :class => 'text-error') : pat(:example)
- error = @worker.errors.include?(:recoord)
%fieldset.control-group{:class => error ? 'has-error' : ''}
  =f.label :recoord, :class => 'control-label'
  .controls
    =f.text_field :recoord, :class => 'input-xlarge input-with-feedback'
    %span.help-inline=error ? f.error_message_on(:recoord, :class => 'text-error') : pat(:example)

.form-actions
  =f.submit pat(:save), :class => 'btn btn-primary'
  &nbsp;
  =f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue'
  &nbsp;
  =link_to pat(:cancel), url(:workers, :index), :class => 'btn'
